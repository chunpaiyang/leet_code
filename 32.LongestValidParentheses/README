這題...不是很難。
但很容易寫錯(漏東漏西)。

這題主要用到 "分 case 考慮" + "Dynamic programming"

另外我少考慮的點有:
1. 多餘的 ) 會中斷連續的 counter
2. 若 ( 的前一個是 ) 那麼 ( 要去繼承前一個 ) 的值


一開始我以為這題很簡單(?太天真)，實際上當題目的要求是"連續"的數目。
其實就要想到 counter 哪時候會被 reset 哪時候要去繼承前面的 counter 並多累計?
以這個為起始就可以解開這個題目了。
